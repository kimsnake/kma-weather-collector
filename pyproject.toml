[project]
name = "kma-weather-collector"
version = "0.1.0"
description = "KMA Weather Data Pipeline"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "dotenv>=0.9.9",
    "pandas>=2.0.0",
    "requests>=2.31.0",
]
